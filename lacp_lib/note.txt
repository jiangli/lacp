a ---- b

change b 's key. then a is not sync , but can't sync again.

so how to tell mux to reselect before select login.
think about make select as a logic in mux




-----------------
    uint32_t slot, port;

        DEV_GetIfPonFromIfnet(argv[1], &slot, &port);
        lacp_ssp_get_global_index(slot, port, &port_index);
